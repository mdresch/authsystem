

# Auth Context

The `AuthContext` (defined in `lib/auth-context.tsx`) provides a convenient way to access the authentication state and interact with Supabase Auth throughout your application.


## Usage

To use the `AuthContext`, import the `useAuth` hook:

```typescript
import { useAuth } from "@/lib/auth-context";